# Design Document

## Purpose
This project implements a console-based Command Line Interface (CLI) as a user interface.

## Architecture
- CliFacade — Facade pattern
- ICommand — Command pattern
- Modes — Strategy pattern

## Extensibility
Commands and modes can be extended without modifying core logic.
